<div class="container">
  <div class="card">
    <ul class="list-group">
      <% @cocktails.each do |cocktail| %>
          <li class="list-group "><%=link_to cocktail.name, cocktail_path(cocktail)%>
          </li>
      <% end %>
      <br>

  </div>
<%= link_to 'Show all drinks', 'www.dif.se' %>
